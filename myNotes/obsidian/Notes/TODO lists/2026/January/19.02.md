---
date: "19.02"
creation time: 11:13
tags:
  - undpence_for_ai
---



# ❗❗❗ must tasks



# ❗❗ should tasks
- [ ] write function at Emitter that reads current byte at buffer
- [ ] write test for function at Emitter that reads current byte at buffer
- [ ] write tests for other helpers
- [ ] write tests for instruction assemblerx86_64.zig functions

# ❗ unshould tasks
- [ ] 
# ✔️ completed tasks
- [x] make ending of a work day in   20:00
- [x] write tests for rex,sib,modrm helpers
- [x] check GMail at V8's developers answer
- [x] at assemblerx86_64.zig find the instruction functions use hand-written code uses primitive helpers(rex,sib,modrm)
- [x] at assemblerx86_64.zig substitute hand-written machine code to advanced helpers that i wrote
- [x] find way to make emitter buffer executable
# ✏️ text for the day



# day without an ai

(no gives 0 points,yes gives 1 points,yes/no 0.5 points)
(if you got >= 1 points your day without an ai)

- the morning without an ai? yes
-  the day without an ai? yes
-  what i've done on the day?:
1. write tests for most important helpers
2. write tests for other helpers
3. found way to call all syscall types in zig 
4. understand for functions generating code need to invoke syscalls that changes memory rights
- what i'm can make better on the tommorow?:
- do [[19.02#❗❗ should tasks]]
- at zig write universal syscall wrapper bottom of raw linux/windows/macos 
# schedule of the day
# prepare(13 minutes )
suggested actions:
- check GMail at V8's developers answer(2 minutes)
- at assemblerx86_64.zig find the instruction functions use hand-written code uses primitive helpers(rex,sib,modrm)(7 minutes)
- write tests for rex,sib,modrm helpers(5 minutes)
- write function at Emitter that reads current byte at buffer(1 minute)
- write test for function at Emitter that reads current byte at buffer(1 minute)
# worm up(60 minutes )
suggested actions:
- at assemblerx86_64.zig substitute hand-written machine code to advanced helpers that i wrote(10 minutes)
- write tests for other helpers(30 minutes)
- write tests for instruction assemblerx86_64.zig functions(20 minutes)
# deep learn(xx )
suggested actions:
- 


## functions at assemblerx86_64 using primitive(sib,rex,modrm) helpers
- instr_div
- instr_push