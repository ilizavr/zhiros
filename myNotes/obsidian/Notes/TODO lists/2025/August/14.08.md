#зависимость_без_ИИ
# ❗❗❗ обязательные задачи

# ❗❗ желательные задачи
- [ ]  доделать ошибку в парсере BPB(см ошибку в коде и прошлых todo) 
# ❗ нежелательные задачи
- [ ] 
# ✔️ выполненные задачи
- [x] прочитать код метода open в пакете sql go
- [x] выбрать шаблон  html/css для ruslan dev
- [x] начать разработку ruslan dev
- [x] сделать таблицу questions через gorm в таблицу ruslandev.db. обязательно закоммитить код создания табоицы на go 
- [x] отполировать код с созданием таблицы через gorm чтобы он выглядел более-менее нормально
- [x] закоммитить код
- [x] сделать код для главной страницы которая будет делать запрос в sql через эндпоинт и выводить его на главной странице
- [x] закоммитить код
# ✏️ конспекты на день

```go
func (db *DB) connectionOpener(ctx context.Context) {
	for {
		select {
		case <-ctx.Done():
			return
		case <-db.openerCh:
			db.openNewConnection(ctx)
		}
	}
}

```



#  вопросы ии
а что нужно чтобы сделать свою бд? просто внешне выглядит просто: 

создаешь прогграму которая слушает определенный порт с привязанным сокетом, принимаешь все запросы, парсишь их,валидишь их, выполняешь построчно что указано в бинарном укзаании.  например создать хэш мапу в памяти. Просто появилось небольшое желание написать свой клон mem cached после создания своей ос. вообще после чтения echo я стал реально более бесстрашным к таким задачам и к чужому коду. я понимаю что боевые бд сложнее, как минимум потому что они работают в нескольких потоках, но вроде бы создать свой mem cached не такая уж и сложная задача, верно? учитывая после  того как я перестал бояться чужого кода после чтения кода echo.




# об gorm
в данном фреймворке метод Exec возращает экземпляр DB, это значит что просто так ответ не получить,  и нужно покапаться вструкутре DB чтобы понять где возращается ответ. по моим предположениям ответ возращается в поле [Statement](https://pkg.go.dev/gorm.io/gorm#DB)


# на завтра
как минимум завтра нужно сделать страницу и код для для эндпоинта создания  вопроса. там будет поля заголовка и описания и белая кнока "create". Эндпоинт для создания будет называться /create. И написать этот эндпоинт ПОЛНОСТЬЮ БЕЗ ИИ

а если ты напишешь /create то пдходи к самой сложной части : просмотру ответов. см конспект в обсидиан на телефоне чтобы понять что делать

О да и завтра утром первым делом проставиь заметки смог ли без ИИ начиная с 13 августа по сегодня

# день без ИИ

- Утро без ИИ? ✔️
- день без ИИ? ✔️
- Что сделал сегодня:
1. я попробовал прочитать код database/sql. что я понял? мне не нравится корпоративный стиль, когда весь код усеян интерфейсами а не самой реализацией
2. я начал разработку ruslan dev
3. сделал 1 из 3 компонентов ruslan dev(главную страницу)
4. освежил знания о git и поиспользовал их снова на практике
- Что улучшить на завтра:
сегодня я написал лишь 1/3 компонентов сайта ruslan dev из-за занятия которое сегодня блин было. в итоге я написал не весь сайт а только его треть. завтра нужно доделать как минимум еще одну треть, а лучше доделать сайт вообще до конца