---
date: "08.11"
creation time: 11:12
---



# ❗❗❗ must tasks
- [x] make daily note
- [x] train
- [x] obtain knowlege about all features of define directive
- [x] understand most deep concepts in next() function, and understand how process hard define forms
- [x] understand tcc 0.1 has very raw code that doesn't ready
- [x] make ending of a work day in   20:00
# ❗❗ should tasks
- [ ] revise code of define directive on "just work " without recursive calls
# ❗ unshould tasks
- [x] draw scheme how work define and undef directive
- [x] see how many directives have in C, read tcc  0.26 code

- [x] check geometry formuls to prepare sveta checking
# ✔️ completed tasks
- [ ] 

# ✏️ text for the day



# day without an ai
- the morning without an ai? yes
-  the day without an ai? yes
-  what i've done on the day?:
1. understand, code of tcc 0.1 very raw and read him is danger
2. understand that, my define algorithm is incorrect
3.  telling with mom and maybe, she stop calling me "stupid"
4. understand new directives in C
- what i'm can make better on the tommorow?:
- do [[8.11#❗❗ should tasks]]
- remove error in preprocessor code about "->" operator
- write function to redefine macroses on him values in preprocessor
- write function to open file . the function is part of include directive
- write function to put file value we are copied
- read problem of homework in yandex lms
- write sysnopsys homework in geography
- revise homework and class work by geography in note book

# time windows

# prepare(52 minutes)
suggested actions:
-  make daily note for 8.11(5 minutes)
- complete train in "home work out" application(30 minutes)
-  draw scheme how work define and undef directive(10 minutes)
- obtain knowlege about all features of define directive(7 minutes)
# worm up(1,5 hour)
suggested actions:
- learn more about recursive(15 minutes)
- revise code of define directive on "just work " without recursive calls(5 minutes on building,10 minutes on implementation)
- write skeleton of include directive without recursive(30 minutes)
- setting vim on omar phone(30 minutes)
- # deep learn(4 hours)
suggested actions:
- understand most deep concepts in next() function, and understand how process hard define forms(1,5 hours)
- revise define directive code  and use recursive(1,5 hours)
- revise include directive code and make the include directive may copy files recursievly(1 hour)

# using of define macros i should implement in pcc

- common using
-  within brackets put some expression
- defining parametrized macros and use parametrs within brackets expression
- use chain  macros(define AB \n define B 100)
- use multi line macroses. to use more than one line, you can use backslash(/)

# algorithm of define
- read next string
- if string is define do next:
1. represent macro table how tree
 2. redefine all macros names on correspond values of macros
 3. do same as with code of project


# вопрос для ии

а что такое теория комиляторов? для компиляторов есть свой раздел математики а ОС почему нет?

как системники относятся к структурам? лично у меня к ним пренебрижительное отношение как будтно они черепах как директивы