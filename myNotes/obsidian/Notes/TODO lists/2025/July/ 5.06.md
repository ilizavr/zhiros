- [x] скачать redis на manjaro
- [x] попробовать redis работает ли он
- [x] установить библиотеку для redis на go 
- [x] попробовать библиотеку для redis на go
- [x] придумать архитектуру мини приложения. делать [тут](https:excalidraw.com)
- [x] реализовать приложение
- [x] внедрить DI 
- [x] написать интеграционые тесты

# директории приложения
```handler``` - API gateway
```UseCase``` -  Go App
```database``` - работа с redis
```test``` - интеграционые тесты


# реализация интеграционых тестов

## интеграционый тест #1

1 интеграционый тест проверяет взаимодейтсвие между  handler.go и UseCase.go

первое что нужно сделать это фейк для redis бд который имплементирцет Storage. если не знаешь что такое фейк см [[что такое фейк в тестировании]]

дальше мы проверяем  функцию SetTODOHandler:
- задаем слайс структур:
```go
tests := struct{
name string
inputData string
expectedStatusCode int
expectedError bool
}
```

после тестовых кейсов делаем следущее:

задаем gin.Default,r.Use(StorageInjector),r.POst(...),new recorder, new reqest, serve http.

вобщем все как в обычных тестах но дальше будет интереснее:

создаем в фейке нцжные данные через set

делаем запрос 

проверяем статус код 

