
#зависимость_без_ИИ 
#блокировка
# ❗❗❗ обязательные задачи

# ❗❗ желательные задачи
- [ ] найти на какой строке расположена функция execpline в коде zsh/exec.c
- [ ] прочитать код исполения пайплайна в zsh
- [ ] исправить баг, что rsh неправильно работает с переменными окружения
# ❗ нежелательные задачи
- [ ] 
# ✔️ выполненные задачи
- [x] исправить баг см [[12.09#день без ИИ]]
- [x] прочитать код функции парсинга строки rsh
- [x] понять почему rsh понимает команду только тогда когда ставишь двойную букву начала названия команды. к тому же если ввести абсолютно неправильную команду, то ничего не будет хотя должно
- [x] исправить баг, что rsh неправильно работает с встроенными командами
- [x] убрать баг с which, который видимо разучился обрабатывать встроенные функции
- [x] убрать дебаг сообщения в функции определния встроенной команды
- [x] подвести итоги дня в конспектах дня в 20:50
- [x] заблокировать ии дипсик и диип аи
# ✏️ конспекты на день


# день без ИИ
- утро без ИИ? да
- день без ИИ? да
-  Что сделал сегодня?:
1. сам  - почти ничего сраный день рождения
- Что улучшить  на завтра?:
- см [[13.09#❗❗ желательные задачи]]. только совсем без ии, еслли будешь с ии я убью тебя!
- вот тебе самому не нравится, как последнее время я особо ничего не придумывал, за меня это делал ии а я понимал его мысль. если я хочу создать что то крутое, так нельзя, иначе я могу потерять фундаментальные навыки что совершенно неприемлимо. я предлагаю тебе : отдыхать эффективно но мало, реально использзовать метод pommodora,   и наконец снова начать использовать расписание дня. плюс пиши тексты (тоесть заметки на день) на английском
- 
# an предположение
мое предположение в том, что парсер rsh начинает с элемента 1, именно поэтому оппускается любая буква первого элемента. честно я не понимаю где это может быть вообще, но попробуй отдеббажить

еще предположение в том, что я работаю с args как с одномерными массивами хотя они вообще двумерные! я вообще сомневаюсь нужен ли двумерный укзаатель in the case!

# о баге с env
я предпологаю что какая то функция работает с buf а не args, именно поэтому где то потом выводится название переменной непосредственно, а не значение

# разблокировать ии
# Очистить все правила
sudo iptables -F

# Восстановить сохранённые правила
sudo iptables-restore /etc/iptables/iptables.rules


# заблокировать ии
используй эту команду чтобы полностью убрать доступ к deepseek:

sudo iptables -A OUTPUT -p tcp -d chat.deepseek.com --dport 80 -j DROP                                     
sudo iptables -A OUTPUT -p tcp -d chat.deepseek.com --dport 443 -j DROP
sudo iptables -A OUTPUT -p tcp -d deepai.com --dport 80 -j DROP
sudo iptables -A OUTPUT -p tcp -d deepai.com --dport 443 -j DROP
sudo iptables-save | sudo tee /etc/iptables/iptables.rules
sudo systemctl enable iptables
sudo systemctl start iptables
sudo iptables -L OUTPUT -n --line-numbers

