---
date: "21.02"
creation time: 10:15
---



# ❗❗❗ must tasks

# ❗❗ should tasks
- [ ] keep in mind a fint: PageAllocator.map() internally uses posix.mmap for full cross-platforming. Need to understand if use POSIX version more safety for corss-platform rather than hoping MacOS implementing same interface as Linux
- [ ] in assemblerx86_64.zig add first parametr as E(the global emitter) at every  helper that uses in instr_* functions
- [ ] write tests for instr_rem/dup and fun_prologue
# ❗ unshould tasks
- [ ] 
# ✔️ completed tasks
- [x] add for all enums at AftonJIT under-slash(_,) for ensure they're not invoke a bug
- [x]  do [[20.02#day without an ai]] tasks
- [x] try substitute in emitter.zig at init function from standard allocation to mmap allocation and correspondly substitute standard free to mmap free
- [x] add in emitter.init() function type cheching of Allocator. and if it PageAllocator(probarly check by @TypeInfo()) then make mmap allocation, in other case make common alloc 
- [x] read the description of @TypeOf builtin;see examples; need to understand if it would  determine an every type involving big structures
- [x] make ending of a work day in   20:00

# ✏️ text for the day



# day without an ai

(no gives 0 points,yes gives 1 points,yes/no 0.5 points)
(if you got >= 1 points your day without an ai)

- the morning without an ai? no
-  the day without an ai? yes
-  what i've done on the day?:
1. understand GPA only work when rights of memory != PROT_WRITE
2. tried to write 2 allocators(GPA and PageAllocator) at Emitter struct that was hard
3. write all tests either for helpers and for instructions(exclude function prologue)
- what i'm can make better on the tommorow?:
- do [[21.02#❗❗ should tasks]]
- planning my own RoofFS FileSystem
# schedule of the day
# prepare(xx )
suggested actions:
- 
# worm up(xx )
suggested actions:
- 

# deep learn(xx )
suggested actions:
- 

# path for allocator at zig has opportunity to allocate mmap page
http://127.0.0.1:44671/#std.heap.PageAllocator