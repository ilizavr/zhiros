
# смысл проблемы
функция для  получения самого эффективного кол ва уроков зависает
# возможное решение

функция для получения элемента  из слайса не удаляет элемент из слайса, из за этого функция работает вечно, возможно функция func RemoveAtStable[T any](s *[]T, index int) {  
    if index < 0 || index >= len(*s) {  
       return  
    }  
    *s = append((*s)[:index], (*s)[index+1:]...)  
} 
не работает ккоректно 


можно сделать чтобы функция возращала новый слайс без  того элемента который ьыл удален. и присвоить слайсу уроков значение нового слайса  
# сообщение для  stack over flow(если очень надо)
