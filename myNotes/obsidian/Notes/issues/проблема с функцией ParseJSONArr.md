
# смысл проблемы
 функция ParseJSONArr парсит строку неправильно если в виде строки передается json строк а не чисел. тоесть например в функцию передается "[\"No\",\"Comments\",\"Found\"]", то функция распарсит этот json не как ["No" "Comments","Found"] а как ["No" "Comments"]. тебе нужно решить эту проблему, и наверное она решится только с помощью gdb
# возможное решение
продебажить  состояние каждой переменной через gdb
# сообщение для  stack over flow(если очень надо)
